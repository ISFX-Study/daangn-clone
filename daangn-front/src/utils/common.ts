/**
 * 공통 함수
 */
const CommonUtils = {
    
    /**
     * 테스트
     * @param a 
     * @param b 
     * @returns a, b의 합계
     */
    add: (a: number, b: number) => {
        return a + b;
    }
}
export default CommonUtils;